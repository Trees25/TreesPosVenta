{
  "empresa": {
    "id": {
      "description": "Note:\nThis is a Primary Key.<pk/>",
      "format": "bigint",
      "type": "integer"
    },
    "nombre": {
      "format": "text",
      "type": "string"
    },
    "moneda": {
      "default": "$",
      "format": "text",
      "type": "string"
    },
    "logo": {
      "format": "text",
      "type": "string"
    },
    "id_auth_user": {
      "format": "uuid",
      "type": "string"
    },
    "id_plan": {
      "description": "Note:\nThis is a Foreign Key to `planes.id`.<fk table='planes' column='id'/>",
      "format": "bigint",
      "type": "integer"
    },
    "dias_alerta_vencimiento": {
      "default": 7,
      "format": "integer",
      "type": "integer"
    },
    "contador_ventas": {
      "default": 0,
      "format": "integer",
      "type": "integer"
    },
    "razon_social": {
      "format": "text",
      "type": "string"
    },
    "cuit": {
      "format": "text",
      "type": "string"
    },
    "nro_iibb": {
      "format": "text",
      "type": "string"
    },
    "punto_venta": {
      "default": 1,
      "format": "integer",
      "type": "integer"
    },
    "id_condicion_iva": {
      "description": "Note:\nThis is a Foreign Key to `condiciones_iva.id`.<fk table='condiciones_iva' column='id'/>",
      "format": "bigint",
      "type": "integer"
    },
    "fecha_inicio_actividades": {
      "format": "date",
      "type": "string"
    }
  },
  "usuarios": {
    "id": {
      "description": "Note:\nThis is a Primary Key.<pk/>",
      "format": "bigint",
      "type": "integer"
    },
    "id_auth": {
      "format": "uuid",
      "type": "string"
    },
    "nombres": {
      "format": "text",
      "type": "string"
    },
    "email": {
      "format": "text",
      "type": "string"
    },
    "id_rol": {
      "description": "Note:\nThis is a Foreign Key to `roles.id`.<fk table='roles' column='id'/>",
      "format": "bigint",
      "type": "integer"
    },
    "nro_doc": {
      "format": "text",
      "type": "string"
    },
    "id_empresa": {
      "description": "Note:\nThis is a Foreign Key to `empresa.id`.<fk table='empresa' column='id'/>",
      "format": "bigint",
      "type": "integer"
    },
    "id_sucursal": {
      "description": "Note:\nThis is a Foreign Key to `sucursales.id`.<fk table='sucursales' column='id'/>",
      "format": "bigint",
      "type": "integer"
    },
    "estado": {
      "default": "activo",
      "format": "text",
      "type": "string"
    }
  }
}